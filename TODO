FILE-FORMAT:
    OK - refazer sistema de salas
    OK - cada "tela" da área é separada individualmente
    - é possível definir na tela se ela tem paredes em N, S, W, E (já seta os tiles para blocked, não deixa mudar)
    OK - ao carregar o mapa no jogo, elas são unidas em uma só sala
    - só permitir salas contnuas no editor de área
    - cria rlinks entre áreas


- GAME-DESIGN:
    # criar primeiro mapa

- HUD:
    # CRÉDITOS: https://opengameart.org/content/golden-ui-bigger-than-ever-edition
    # mostrar arma #1 e #2
    # mostrar items (item vai ter que ter um ícone)

- menu:
    # adicionar L/R para mudar tela
    # desenhar fundo tela menu (moldura)
        # (default start) upgrades, items e armas
        # (default select) mapa
        # options

- npc:
    # testar
    # is_on_screen - adicionar suporte a scroll.y

- player:
    # is_on_screen - adicionar suporte a scroll.y

- implementar link entre mapas
    # deve ser parte das áreas
    # ao carregar mapa no jogo, cria os links automaticamente
    # simplificar transição

- corrigir hitbox: deve ser baseada no frameSize e porcentagens

- criar/implementar area-teleporter

- melhorar performance editor preview-areas
    # carregar imagem na função que muda filename

- remodelar animation: deve ser capaz de retornar a área da imagem que deve ser mostrada
    # responsável por mostrar imagem deve ser GameController/ImageView
    # lista de animações deve ficar no gameController, não no mapa

- remover armas e seleção L/R e menu
    # remover item coil/jet

- EDITOR: não pode ter largura ou altura menores do que o tamanho de tela (descontando HUD)

- ajustar scroll-y quando restaurar posição em checkpoint


