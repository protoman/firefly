========================================================================================================================
Clean the Box2DManager from unused variables and functions.
Then, better organize the code so it is easier to read for developers. Add documentation when some part is not clear.

========================================================================================================================

Based on the shape and variables used by the player, in the Box2dManager class, I want to change it so that
the Box2dManager class should hold Box2D variables required for the character boxid, shape or state in a map structure,
the key to the map will be the character id, this includes the player, so refactor the existing variables too.
Also, create a method that builds the shapes, ids and other necessary objects, and add it to this map.
The method should receive required parameters such as, initial position on the map, size, etc.

========================================================================================================================
On the GameEnemy and GameNpc classes, when initializing the NPC, call the method in Box2dManager that creates
the id and shape objects into a map, passing all the required parameters, so all required box2d variables,
exist for that character.
========================================================================================================================

Now, for the GameObject class too, when initializing the object, call the method in Box2dManager that creates
the id and shape objects into a map, passing all the required parameters, so all required box2d variables,
exist for that character.
========================================================================================================================

Remove inertia, if used, from classes character, GameNpc, GameEnemy and artificial_inteligence.
It should rely on Box2DManager for that character in the map on that class, for speed and movement instead.
========================================================================================================================
Remove moveSlopes, if used, from classes character, classplayer, GameNpc, GameEnemy and artificial_inteligence.
It should rely on Box2DManager for that character in the map on that class, for speed and movement instead.
========================================================================================================================

Remove jump class, if used, from classes character, classplayer, GameNpc, GameEnemy and artificial_inteligence.
It should rely on Box2DManager for that character in the map on that class, for speed and movement instead.
========================================================================================================================
If you hold the jump button, once the jump ends, a new one is started after greaching ground.
It should only start a new jump if the user releases the jump button, and press it again,
after the player hits the ground;
========================================================================================================================
Sometimes, when on a slope, if you press the jump button, the jump is very short,
instead of going all the way up. Create unit-tests to try to reproduce the issue and then create a fix.

========================================================================================================================


========================================================================================================================

# animações novas para sprites:
    # agachado
    # levando dano
    # morrendo


# frankie
    # ataque:

# lobisomem
    # adaque: slash (curta distância)
    # abilidade especial: agarrar paredes (como mmx)

# múmia
    # ataque: bandagens (média distância, similar a chain do spike)
    # abilidade especial: ?

# beta
    # ataque: duplo tiro carregado
    # abilidade especial: sobreviver no espaço
