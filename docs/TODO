########################################################
                     TODO LIST
########################################################


Validade this tool for paint to replace paint tool sai:
https://medibangpaint.com/en/
https://krita.org/pt-br/


- Each player sprite needs its own size (w/h)
    # setting the sprite must be done ONLY through a method, so it can update the size in box2d
    # improve the way we place the player sprite on screen
    # move player position in player class to player-shared-data, use box2d only for the movement
    # add an "execute" method in box2d that moves player/etc

- Split SDL3 code better
    # there should be no SDL code outside SDL-layer ones.
    # minimize the use of SDL functions, avoiding duplication.

- Organize data with JSon
    # npcs
    # quests
    # enemies

- After data is organized, editors will be separate projects
    # player, objects, projectile and weapons can be hardcoded as in old times
    # on first run, ask where is data directory
    # name is the link between tiled and the data
    # enemy (including AI)
    # friendly npc -> include dialogs
    # cutscenes (movie)

- Configure build system on github

- remove all build warnings
- use a tool for code-cleaning like SonarCube

- move to SDL3

- created a method that will determine after witch layer the player will be added
    # it should look for a "PLAYER" layer
    # if none is found player should be draw before the tileset layer

- move all the scrolling logic from map to its own class
    # scroll and scrolled can be stored in shared_map_data

- split data into a sub-repo
- create an "open" data with placeholders, and then an official data repo that is private

- Refactor map::draw
    # loop through layers, draw each one in order
        # methods for image, object and tileset layers
    # that way we can have infinite image, object and tileset layers
    # need to figure out how to draw player, probably after first objects layer


Fazer um arquivo de "shared-data" para cada tipo de coisa.
Por exemplo:
    # Map-Data (tileset, tiles, image-layers, etc)
    # Enemies data (textura, posição, estado, etc)


Resolution indepedance
    # https://gamedev.stackexchange.com/questions/177894/what-is-the-proper-way-of-handling-resolution-and-size-units-on-a-2d-game


separar em headersa diferentes st_rectanble, st_point, st_size, etc

##########################################################################################################################################

https://discourse.libsdl.org/t/box2d-and-sdl2-no-collision-after-rendering/26436/5
https://github.com/obiot/mario-2d-jumps-box2dwasm-melonjs
https://github.com/eder13/Box2D-SDL2-Demo
https://forums.solar2d.com/t/box2d-or-custom-collision-system/354054

- Player movement: https://www.iforce2d.net/b2dtut/constant-speed

https://store.steampowered.com/app/1802190/Puzzles_For_Clef/

https://github.com/obiot/mario-2d-jumps-box2dwasm-melonjs

https://www.iforce2d.net/b2dtut/jumping

https://www.youtube.com/watch?v=uaIQau4woSM



########################################################
                     REFACTOR
########################################################
- IA deve incluir máquina de estados (ver em que pé parei)
- mover detect_language da main para uma classe

########################################################
                     PRIORIDADE
########################################################
- usar Box2D e Tiled para mapas e movimento/colisao.
    # https://lyze.dev/2021/03/25/libGDX-Tiled-Box2D-example-tiles/

========================================================
5. usar json como formato de arquivo para guardar dados
    # vantagem é flexibilidade, pois não usa número limitado de dados (array)
    # por exemplo, número de foregrounds e backgrounds de tela não precisa ser fixo
========================================================
- no firefly, cada anim-type deve ter sua vulnerable-area (opcional)
    # fazer colisão com polígonos -> ver se vale usar uma lib
    # poder setar "is-jump-frame", "is-turn-frame" (daí não precisa a imagem ter o mirror), etc. Pode chamar de key-frame.

- não armazenar imagem dos sprites, só a posição lógica deles, assim não duplica se repetir frames

- reorganizar dados
    # centralizar em uma classe, GameData que tanto o editor como o jogo usam,e  tem load/save como o mediator,
    # passar coisas do sharedData, mediator e gamemediator para esta nova classe.
    # a própria classe tem os métodos de save e load.
    # shareddata passa a conter apenas os objetos in-game/editor.

- arrumar mini-mapa e mapa do menu

- algumas vezes (pular nas 2 plataformas e voltar) a movimentação X fica bloqueada pelo site tipo platform
    # tem que setar para não bloquear no xinc tb.

- novos tipos de objetos:
    # mensagem (tipo placas)
    # give item (dá outro objeto, um de quest)
        # pode ter uma mesg custom ao receber o objeto
    # objeto de quest (tipo npcs, só que é um objeto qe dá outro qdo recebe um)

- Começar a pensar básico da primeira fase.
    # plataforma intangível de baixo para cima e lados
        # só bloqueia Y qdo yinc > 0 e px+p.h >= tile.x
        # permite pular para baixo
    # pântano com area-movediça
    # subterrânemos com esgotos ou algo assim


- GAME:
    - npcs x enemies
    - doors

- novos tipos de animações:
    GOT_ITEM
    CROUCH
    FRONT (para salvar, etc)

- animações NPC (3 frames cada):
    # STAND
    # WALK
    # GOT_ITEM
    # GIVE_ITEM
    # TALKING


- Quests editaveis (é melhor)
- Pensar como fazer diálogos.

- EDITOR: adicionar opção para re-gerar mapas do stage

- EDITOR: clicar novamente em uma sala já na lista esta corrompendo mapa
    # deveria ignorar e apenas salvar a solução da seleção (para definir propriedades só daquela sala)
    # além disto tem algumas salas com azul mais claro; será que estão duplicadas noa rquivo?
    # mostrar a sala selecionada com uma borda vermelha

- tentar importar correções do Rockbot que forem relativas à hitbox/hit-area/etc
    # só fazer na direção certa no preview

- definir como vai funcionar primeiro estágio e suas features
    # cada estagio tem que ser como um mini-game
        # nenhum item de escambo é usado em outra área
        # nenhuma quest é feita entre estagios
    # terreno tipo areia-movediça
    # elevador
    # sala de save e troca de personagem
    # personagens e suas abilidades
    # items que dão novas abilidades
    # items der escambo
        # ideia é ter que ir e vir usando items
    # mapa (inspiração super metroid agora no início)


# ver que libs poderiam ser adicionadas ao jogo, como colisão e mapa
    # https://github.com/SolarLune/resolv
    # efeitos de luz/sombra
        # https://discourse.libsdl.org/t/is-possible-to-create-ambient-light-in-sdl2/28381/2

# ver se vale a pena usar openGL para efeitos de luz/sombra

########################################################
                        BUGS
########################################################
- colisão ao mover esquerda/direita não esta bloqueando avanço


########################################################
                    FEATURES NOVAS
########################################################
- não precisa ter um mapa geral
    # mapa geral (stages) pode ser hardcoded
        # tem pontos (áreas) que podem ser abertos e daí você vê o mapa dela
    # link entre stages sempre é do tipo teleport
        # remover do editor os demais
    # gerador de mapa deve saber onde tem itens do tipo teleport, save-point, etc
        # editor não deve permitir ter dois itens do tipo na mesma sala
    # ao carregar tileset de salas, deve criar uma mapa apenas com os tiles que tenham tile ou terrain

=> procurar /// @TODO
    # tem várias coisas de map-links que removi

- fundos animados
    # opção de frames X e frames Y
    # por padrão é 1x1, se aumentar, divide a tela em partes horizontais ou verticais

========================================================
- independência de resolução/proporção de tela
    # ter assets 1080p, 720p e 480p
    # tilesize ajustável de acordo
    # multiplicador de movimento (em sharedData)
    # tamanho da fonte de acordo
    # suporte a diferentes proporções de tela (?)

########################################################

- limpar editor:
    # tirar tudo de scenes, já que vai ser tudo hard-coded de agora em diante.
    # tirar coisas de scenes do file-format tb (file_scene*)

- EDITOR:
    # desenhar overlay (como transparente)
    # opção para não mostrar overlay

- fazer link entra áreas

- corrigir slopes (quando descendo ele "quica" nos solid q tem entre-meios)

- (melhorar) joystick rumble

- items que não são plataforma não devem parar porque estao em colisão com personagens

- EDITOR: trocar locks de cores para generico, por objeto

QUEST:
    # está duplicando item após ele ser transformado
        # uma vez que um item tenha sido transformado, tem que indicar que ele não pode existir mais

    # lista de estado de cada npc (lista, gravada junto com save-game)
        # cada npc tem uma quest atômica, ou seja, recebe um item, dá um item em retorno
        # o mesmo pode ser aplicado para ports/objetos

EDITOR:
    # mudar W/H quando troca gráfico
    # isNPC true, HP = 99
    # HP padrão 2
    # não pode adicionar no JOGO (é, inteiro) dois npcs com mesmo id


#######################################################################################################
Créditos:
#######################################################################################################
Credit "Tio Aimar @ opengameart.org" or simply "Tio Aimar" (this is not mandatory)
Credit "Kenney.nl" or "www.kenney.nl", this is not mandatory.
HUD: https://opengameart.org/content/golden-ui-bigger-than-ever-edition

#######################################################################################################
i18n:
#######################################################################################################
ver se tem alguma lib ou esquema de SDL para ajudar
adicionar suporte a level no arquivo de save de posição de objetos
adicionar método que, ao usar um game-item, se tem um objeto no local, pode dar trigger nele
#######################################################################################################

#######################################################################################################
Sistema de uso de item
#######################################################################################################
- Fazer hardcoded em parte
- a definição de qual item é usado é setada pelo editor
- mas a forma, é hardcoded
- para isso precisamos de um sub-tipo de item, definido em enum mas que vai ir aumentando

#######################################################################################################
BUGS
#######################################################################################################
====> - corrigir posição do texto no menu de quit
====> - corrigir GFX que estão quebrados após separação de layers game e hud
- EDITOR: não está salvando projétil, sempre volta para o default

#######################################################################################################
FEATURES
#######################################################################################################
====> - adicionar rosto aos dialogos in-game
====> - terreno que faz dano (não como spike, como tocar um inimigo)
- portas tipo barreira que são abertas por um botão (tipo iconoclasts)
- plataforma fina que permite pular para baixo
- efeito sobre uma única camada que rotaciona ele (para fazer coisas como árvore balançando)
- transparência real ao ser atingido (idem inimigos)
- colocar nome (seu/do-save) ao criar novo jogo
- elevadores
- fazer chicote e inimigos segmentados, cujas partes obedecem função matemática de movimento (pode ser hardcoded sem problema, faz filho da classe classBoss ou classNpc)
- usar lib de particulas para explosões e cia
- animação de armas e inimigos usando rotação
- inimigos com sombra (tipo o dash, mas com gráfico próprio para a sombra)
- criar dark room (apenas círculo ao redor do jogador é mais transparente)
- não poder usar ALT como tecla (está para alternar fullscreen)

#######################################################################################################
FEATURES (DEPENDE ARIS)
#######################################################################################################
- intro
- tela game-start


- Converter para modo "async" de atualização da tela (como diálogos).
  Usar flags para indicar que está trancado esperando a execução de alguma coisa (pensar como fazer a flag)
    # draw::show_ready
    # draw::show_credits_text
    # draw::show_ingame_warning
    # character::execute_jump_up
    # character::execute_jump
    # character::fall


#######################################################################################################
OPTIMIZAÇÃO
#######################################################################################################
- criar lista de inimigos, projéteis e objetos que estão na tela e próximo dela
- melhorar performance editor preview-areas
    # carregar imagem na função que muda filename



- GameObject::gravity: remover quando fora do mapa
- está pegando item ao voltar do teleporte de área

- GAME-DESIGN:
    # criar primeiro mapa

- HUD:
    # mostrar arma #1 e #2
    # mostrar items (item vai ter que ter um ícone)

- menu:
    # desenhar fundo tela menu (moldura)
        # mostrar chaves no in-game-menu
        # (default start) upgrades, items e armas
        # (default select) mapa
        # options

- npc:
    # testar
    # is_on_screen - adicionar suporte a scroll.y

- player:
    # is_on_screen - adicionar suporte a scroll.y

- implementar link entre mapas
    # deve ser parte das áreas
    # ao carregar mapa no jogo, cria os links automaticamente
    # simplificar transição

- corrigir hitbox: deve ser baseada no frameSize e porcentagens

- criar/implementar area-teleporter (???)


- remodelar animation: deve ser capaz de retornar a área da imagem que deve ser mostrada
    # responsável por mostrar imagem deve ser GameController/ImageView
    # lista de animações deve ficar no gameController, não no mapa

- EDITOR: não pode ter largura ou altura menores do que o tamanho de tela (descontando HUD)

- ajustar scroll-y quando restaurar posição em checkpoint


